#include <stdio.h>
int main()
{
     /*float x, n, d, t = 1, sum = 1;
    printf("Enter the value of x:\n");
    scanf("%f", &x);
    printf("Enter the number of terms:\n");
    scanf("%f", &n);
    for (int i = 1; i <= n; i++)
    {
        d = (2 * i) * (2 * i - 1);
        t = -t * x * x / d;
        sum = sum + t;
    }printf("sum is\n",sum);*/
    int a=2,x=1,y=5,b=3,c=4;
    b !=!a;
    c !=! a;
     
    printf("%d %d ",c,b);
    return 0;
}